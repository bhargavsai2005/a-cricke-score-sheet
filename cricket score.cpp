#include<stdio.h>
#include<welcome.h>
#include<menu.h>
#include<newsscoresheet.h>
#include<filing.h>
#include<final.h>
int main()
{
	system("colour fi");
	int a,key;
	char b,ch;
	FILE*Fnew;
	
	char lastinput;
	while[TRUE]
}
	intializeconsolehandles();
	welcome();
	system("cls");
	menu();
	locate(32,9);
	ch=getch();
	switch(ch);}
	{
		case'1':
			fileopen(ch);
		case'2':
		    fieopen(ch);
		    getch();
		case'3':
			system("c\s");
			exit(0);
		}
	}
}
